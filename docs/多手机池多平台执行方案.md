# 多手机池多平台执行方案

## 目标

把当前“单实例、单平台、串行执行”的实现，改造成“多手机池 + 多平台串行 + 单平台多手机并发”的批处理执行模型。

最终目标：

- 最多支持 5 台无影云手机组成设备池
- 执行时可选具体设备，例如 `A,B,C`
- 执行时可选多个平台，例如 `doubao,deepseek,kimi`
- 同一时刻每台手机只能执行一个任务
- 平台之间固定串行
- 同一平台下，所选手机并发执行
- 等当前平台当前 prompt 的所有手机执行完成后，才能进入下一步
- 每个平台同一轮的多手机结果保存到同一个汇总 JSON

## 已确认约束

1. 某台手机在某个平台失败或超时后，后续平台仍然继续尝试这台手机
2. 平台固定按传入顺序串行执行，不做平台并发
3. 多 prompt 的执行顺序固定为：

```text
平台 -> prompt -> 设备并发
```

示例：

```text
platforms = [doubao, deepseek, kimi]
devices = [A, B, C]
prompts = [p1, p2]

执行顺序：
1. doubao + p1 -> A/B/C 并发
2. doubao + p2 -> A/B/C 并发
3. deepseek + p1 -> A/B/C 并发
4. deepseek + p2 -> A/B/C 并发
5. kimi + p1 -> A/B/C 并发
6. kimi + p2 -> A/B/C 并发
```

## 当前问题

当前代码只有这些能力：

- 单平台执行：`run_platform_once(...)`
- 单实例执行：默认只取 `WUYING_INSTANCE_IDS` 的第一个实例
- API 任务模型只支持一个 `instance_id`
- 任务服务是单 worker、单任务串行执行

因此当前不能表达：

- 多设备选择
- 同平台多设备并发
- 平台级汇总结果
- 设备占用和租约控制

## 设计原则

- 不重写各个平台 workflow
- 平台差异逻辑继续留在各自 workflow 内
- 多手机池能力只加在“应用编排层”
- 原始单机结果继续保留
- 新增批次级汇总结果
- 先兼容现有单机 CLI/API，再新增批处理能力

## 目标架构

### 设备池配置层

新增设备池配置文件，建议路径：

`config/device_pool.json`

建议结构：

```json
[
  {
    "device_id": "A",
    "instance_id": "acp-aaa",
    "adb_endpoint": "1.1.1.1:100",
    "enabled": true
  },
  {
    "device_id": "B",
    "instance_id": "acp-bbb",
    "adb_endpoint": "1.1.1.2:100",
    "enabled": true
  },
  {
    "device_id": "C",
    "instance_id": "acp-ccc",
    "adb_endpoint": "1.1.1.3:100",
    "enabled": true
  }
]
```

说明：

- `device_id` 是业务侧选择设备时使用的短 ID
- `instance_id` 是无影实例 ID
- `adb_endpoint` 是该手机对应的 ADB 地址
- `enabled=false` 的设备不会被调度

### 设备锁层

新增设备租约管理器，负责：

- 任务开始前锁定指定设备
- 任务结束后释放设备
- 避免两个批任务同时抢同一台手机

推荐能力：

- 支持批量锁定：`A,B,C`
- 如果任一设备已被占用，整个批任务直接拒绝
- 支持异常退出后的锁释放

### 批任务编排层

新增批任务编排器，负责：

- 平台串行
- prompt 串行
- 同平台同 prompt 下设备并发
- 汇总平台结果
- 写平台汇总 JSON
- 维护任务状态

执行模型：

```text
for platform in platforms:
  for prompt in prompts:
    selected_devices 并发执行
    等所有设备结束
    写 platform + prompt 汇总 JSON
```

### 单设备执行层

单设备执行继续复用现有：

- `run_platform_once(...)`
- 子进程超时保护
- 各平台 workflow

这里只做一件事：

- 给每个单设备执行显式传入 `instance_id + adb_endpoint`

## 数据模型

### DevicePoolEntry

字段建议：

- `device_id`
- `instance_id`
- `adb_endpoint`
- `enabled`

### BatchJob

表示一次总任务。

字段建议：

- `job_id`
- `platforms`
- `device_ids`
- `prompts`
- `repeat`
- `status`
- `created_at`
- `started_at`
- `finished_at`
- `error`

### PlatformPromptBatch

表示“某个平台 + 某个 prompt + 多设备并发”的一轮执行。

字段建议：

- `job_id`
- `platform`
- `prompt`
- `prompt_index`
- `device_ids`
- `status`
- `started_at`
- `finished_at`
- `output_path`

### DeviceRunResult

表示某一轮里某一台设备的结果。

字段建议：

- `device_id`
- `instance_id`
- `adb_endpoint`
- `status`
- `result_path`
- `error`
- `started_at`
- `finished_at`

## 结果落盘规范

### 平台汇总结果

新增批次目录：

```text
data/batches/<job_id>/<platform>/repeat_001_prompt_001.json
data/batches/<job_id>/<platform>/repeat_001_prompt_002.json
```

批量模式只在平台汇总 JSON 里保存完整回答，不再额外保存 `data/runs/*.json` 单机副本。

单个汇总 JSON 结构：

```json
{
  "job_id": "batch-001",
  "platform": "doubao",
  "prompt": "你好",
  "prompt_index": 1,
  "device_ids": ["A", "B", "C"],
  "status": "partial_failed",
  "started_at": "2026-04-16T08:00:00+00:00",
  "finished_at": "2026-04-16T08:00:30+00:00",
  "results": [
    {
      "device_id": "A",
      "instance_id": "acp-aaa",
      "adb_endpoint": "1.1.1.1:100",
      "status": "succeeded",
      "result_path": null,
      "error": null,
      "result": {
        "platform": "doubao",
        "device_id": "A",
        "prompt": "你好",
        "response": "AI 回答正文",
        "references": {}
      }
    },
    {
      "device_id": "B",
      "instance_id": "acp-bbb",
      "adb_endpoint": "1.1.1.2:100",
      "status": "timeout",
      "result_path": null,
      "error": "Crawler record timed out after 180s"
    },
    {
      "device_id": "C",
      "instance_id": "acp-ccc",
      "adb_endpoint": "1.1.1.3:100",
      "status": "failed",
      "result_path": null,
      "error": "UI selector not found"
    }
  ]
}
```

## 状态模型

### 单设备状态

- `queued`
- `running`
- `succeeded`
- `failed`
- `timeout`

### 平台批次状态

- `queued`
- `running`
- `succeeded`
- `partial_failed`
- `failed`

### 总任务状态

- `queued`
- `running`
- `partial_failed`
- `failed`
- `succeeded`

状态判定建议：

- 当前平台当前 prompt 下，全部成功：`succeeded`
- 部分成功部分失败：`partial_failed`
- 全部失败：`failed`
- 总任务最终状态按所有批次汇总计算

## 超时和错误策略

- 单台手机超时：只标记这台设备当前执行为 `timeout`
- 单台手机异常：只标记这台设备当前执行为 `failed`
- 不因为某台手机失败而中断其他手机
- 不因为某个平台失败而跳过后续平台
- 后续平台仍然继续尝试已失败过的手机
- 当前平台当前 prompt 必须等所有选中手机结束，才能进入下一步

## CLI 设计

保留统一入口 `run.py app`，新增批处理参数：

```bash
python run.py app --platform doubao,deepseek,kimi --devices A,B,C --prompt "你好"
python run.py app --platform doubao,kimi --devices A,C --file prompts.txt
```

建议新增参数：

- `--devices A,B,C`
- `--device-pool-file config/device_pool.json`

CLI 输出应改成批任务视角：

- 输出 `job_id`
- 输出所选平台
- 输出所选设备
- 输出每个平台每个 prompt 的汇总 JSON 路径

## API 设计

### 保留兼容

保留现有单平台接口，不破坏当前 GEO-watcher 接入：

- `POST /api/v1/tasks/{platform_id}`

### 新增批任务接口

新增：

- `POST /api/v2/batches`
- `GET /api/v2/batches/{job_id}`
- `GET /api/v2/batches/{job_id}/results`

请求体建议：

```json
{
  "platforms": ["doubao", "deepseek", "kimi"],
  "device_ids": ["A", "B", "C"],
  "prompts": ["问题1", "问题2"],
  "repeat": 1
}
```

返回：

```json
{
  "job_id": "batch-001",
  "status": "queued"
}
```

状态接口返回：

- 当前总任务状态
- 当前运行到哪个平台
- 当前运行到哪个 prompt
- 每个平台每个 prompt 的汇总结果路径
- 每台设备的当前状态

## 代码改造范围

建议新增文件：

- `config/device_pool.json`
- `src/wuying/application/device_pool.py`
- `src/wuying/application/device_lease.py`
- `src/wuying/application/batch_models.py`
- `src/wuying/application/batch_runner.py`

建议修改文件：

- `src/wuying/config.py`
- `src/wuying/application/runner.py`
- `src/wuying/application/crawler_tasks.py`
- `src/wuying/interfaces/cli.py`
- `src/wuying/interfaces/api.py`
- `src/wuying/models.py`

## TodoList

1. 新增设备池配置文件 `config/device_pool.json`，定义 5 台手机的 `device_id / instance_id / adb_endpoint / enabled`
2. 在 `config.py` 中增加设备池配置加载逻辑，支持读取设备池文件
3. 新增 `device_pool.py`，实现设备池读取、设备筛选、设备选择校验
4. 新增 `device_lease.py`，实现设备租约锁，防止同一设备被多个任务同时占用
5. 定义批任务模型 `BatchJob / PlatformPromptBatch / DeviceRunResult`
6. 在 `models.py` 中补充批任务相关数据结构和序列化逻辑
7. 把 `runner.py` 从“单实例运行器”扩展为支持显式设备对象执行
8. 确保单设备执行时可以显式使用该设备对应的 `instance_id + adb_endpoint`
9. 保持现有平台 workflow 不改业务行为，只接收外部传入的设备上下文
10. 新增 `batch_runner.py`，实现“平台串行、prompt 串行、设备并发”的主调度器
11. 为同一平台同一 prompt 的多设备执行增加并发控制，推荐使用线程池
12. 在线程池内部继续复用现有单设备子进程超时机制
13. 为每台设备执行结果补充 `device_id / adb_endpoint / status / error / result`
14. 定义单设备状态和平台批次状态的判定规则
15. 新增 `data/batches/<job_id>/<platform>/repeat_xxx_prompt_xxx.json` 的汇总落盘逻辑
16. 批量模式只保留平台汇总结果，不额外写 `data/runs/` 单设备副本
17. 在任务状态文件中增加批任务视角的数据结构，而不是只保存单平台单实例结果
18. 改造 `TaskStore`，支持保存总任务、平台批次和设备结果
19. 改造 `CrawlerTaskService`，支持批任务提交而不是只支持单平台任务
20. 在任务执行时先申请设备锁，任务结束后释放设备锁
21. 如果所选设备中存在被占用设备，任务提交时直接拒绝或返回冲突状态
22. 改造 CLI 参数，增加 `--devices` 和 `--device-pool-file`
23. 改造 CLI 输出，显示总任务、平台批次和汇总结果路径
24. 保留旧 CLI 的单实例兼容调用方式
25. 保留旧 API `POST /api/v1/tasks/{platform_id}` 的兼容能力
26. 新增批任务 API：`POST /api/v2/batches`
27. 新增批任务查询 API：`GET /api/v2/batches/{job_id}`
28. 新增批任务结果 API：`GET /api/v2/batches/{job_id}/results`
29. 为 API 请求体增加 `platforms / device_ids / prompts / repeat`
30. 为 API 返回体增加批任务状态、当前平台、当前 prompt、汇总文件路径
31. 统一 callback 输出结构，确保后续如需对接 GEO-watcher 时可以按批次或单条结果扩展
32. 为批任务执行增加详细日志，至少包含平台开始、prompt 开始、设备开始、设备结束、设备失败、平台汇总完成
33. 为批任务执行增加总超时和单设备超时区分
34. 为批任务执行增加异常回收，保证设备锁一定释放
35. 为多设备并发执行增加本地验证用例，至少覆盖全成功、部分失败、单设备超时
36. 为批任务 API 增加基本联调测试，验证状态查询和结果落盘
37. 更新 README，补充多设备池 CLI/API 用法
38. 更新部署文档，补充设备池配置文件的部署方式

## 备注

这次改造的重点不是平台页面逻辑，而是“调度模型、设备池、状态管理、结果汇总”。

单个平台 workflow 现在已经能跑，真正需要重构的是：

- 任务模型
- 设备模型
- 调度器
- 汇总落盘
- CLI/API 入参与状态输出
